# 2026-09-04 — Integrate DLT/agentic-factory research into chapter draft

**Triggering request:** Immediately following the `deep-book-research` pass (`2026-09-04-1-public-dlt-agentic-factories`), the user confirmed drafting the chapter now, in the same session, rather than deferring it. Invoked `edit-chapter` to resolve the chapter's existing `CONTENT-KEY-SUBJECTS` markers into finished prose.

**Source:** `notes/2026-09-04-1-dlt-sw-factories-public-dlt-agentic-factories/raw-public-dlts--what-it-is--when-to-use-in-general--public-dlts-for-agentic-fabrics--why.md` and its accompanying summary — the deep-research pass run immediately prior in this same session.

**Scope:** All 9 pre-existing `CONTENT-KEY-SUBJECTS` markers in `src/niche-topics/dlt-sw-factories.md` (Public DLTs > What it is / When to use in general; Public DLTs for Agentic Fabrics > Why / Why specifically for Software Fabrics / Which DLT network to use > Requirements / Suitable existing Networks / Relevant protocols and standards), since the file already carried a one-to-one marker structure matching the research's subtopic breakdown — the planning/preview gate (steps 1–5 of `edit-chapter`) was skipped per the skill's own "already-annotated file" fast path, going straight to resolving markers into prose (step 6).

**Structural change made in this pass:** promoted `Requirements`, `Suitable existing Networks`, and `Relevant protocols and standards` from H3 to H4, nesting them under `Which DLT network to use` (previously all four were sibling H3s with no nesting, which didn't reflect that the first three are the sub-questions "Which DLT network to use" is actually asking).
